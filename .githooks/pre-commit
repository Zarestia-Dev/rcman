#!/usr/bin/env bash
set -euo pipefail

# Run clippy on all targets/features, fail on warnings
if ! command -v cargo >/dev/null 2>&1; then
  echo "cargo not found; skipping clippy"
  exit 0
fi

echo "🔎 Running cargo clippy (warnings = errors) ..."
cargo clippy --all-targets --all-features -- -D warnings

echo "✅ Clippy passed"
